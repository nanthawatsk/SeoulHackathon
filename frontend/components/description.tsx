export default function Description() {
    return (
        <div>
        <h2 className="text-2xl font-bold text-center">Description</h2>
        <p>
            ACS Accident Detection is a web application that allows users to upload and display videos. The application is built using Next.js, Tailwind CSS, and TypeScript. The application uses the Next.js API routes to handle file uploads and serves the uploaded files from the public directory. The application also uses the Next.js Image component to display the uploaded images.
        </p> 
        </div>
    );
    }